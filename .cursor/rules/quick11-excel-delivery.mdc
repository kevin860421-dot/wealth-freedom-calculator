# Quick-11 Excel 產出交付

當本專案 **產生或更新任何 Excel（.xlsx）範本**（含 `scripts/generate-quick11-loan-excel.mjs` 或未來新增分頁腳本）時：

## 必做

1. **執行生成腳本**（例：`npm run generate:quick11-excel`）。
2. **交付到使用者下載資料夾**：腳本結尾須呼叫 `deliverExcelToUserDownloads`（`scripts/deliver-excel-to-user-downloads.mjs`）。
   - 預設路徑：**`D:\下載`**
   - 可覆寫環境變數：`EXCEL_USER_DOWNLOADS_DIR`
3. 告知使用者唯一路徑：**`D:\下載\quick11-home-v5-dual-sheets.xlsx`**（或更新後的 `.xlsm`）。**勿**複製到 `%USERPROFILE%\Downloads` 等其他資料夾。

## 產物路徑（三份一致）

| 用途 | 路徑 |
|------|------|
| 站內 API 讀取 | `assets/downloads/quick11-loan-dti-template.xlsx` |
| 靜態直連 | `public/downloads/quick11-loan-dti-template.xlsx` |
| **使用者本機預覽** | **`D:\下載\`（或 EXCEL_USER_DOWNLOADS_DIR）** |

## 禁止

- 僅改 `assets/` 而不跑生成、不複製到 `D:\下載` 就當完成。
- 複製到 `C:\Users\…\Downloads` 或除 **`D:\下載`** 以外的任何使用者下載路徑。
