/**
 * GameFi 錢包服務入口（Phase 1A 實作於 lib/auth/require-user.ts，此處為穩定 import 路徑）。
 */
export {
  INITIAL_GRANT_REQUEST_ID,
  INITIAL_WALLET_GEMS,
  provisionUserWithWallet,
} from "@/lib/auth/require-user";
